# Symlink (CLAUDE.md -> AGENTS.md)
CLAUDE.md

# Local settings (never commit personal config)
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/
.venv/
venv/

# Curated public profiles ARE committed — they are the site corpus.
!profiles/.gitkeep

# Source-repo clones for corpus refresh (scripts/corpus.py). Rebuilt on demand, never committed.
.corpus/

# BMad / local scratch and build output (site is rebuilt in CI, not committed)
_bmad-output/

# Memory (auto-generated, user-specific)
.claude/projects/
